import { Router } from 'express';

const router = Router();

<% if (educational) { %>// Health-check endpoint — useful for monitoring tools and load balancers
// to verify the server is running. Returns a simple { ok: true } response.
<% } %>router.get('/', (_req, res) => {
  res.status(200).json({ ok: true });
});

export default router;
